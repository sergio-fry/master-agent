# Copy to /opt/master-agent/.env during install (install.sh generates passwords).
# Do not commit real credentials to git.

VERSION=0.0.0
TICK_INTERVAL=30s
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-me
SESSION_SECRET=change-me
# Optional: Bearer token for API clients when admin login is also enabled
# MASTER_AGENT_TOKEN=
